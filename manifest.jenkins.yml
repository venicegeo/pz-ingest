applications:
- name: pz-ingest
  host: pz-ingest
  path: pz-ingest.jar
  buildpack: java_buildpack_offline
  domain: stage.geointservices.io
  memory: 2048M
  instances: 1
  timeout: 80
  services:
    - pz-kafka
    - pz-blobstore
    - pz-postgres
    - pz-mongodb
